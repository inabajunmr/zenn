"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.mdMermaid = void 0;
function mdMermaid(md) {
    var defaultRender = md.renderer.rules.fence ||
        function (tokens, idx, options, env, self) {
            return self.renderToken(tokens, idx, options);
        };
    md.renderer.rules.fence = function (tokens, idx, options, env, slf) {
        var langInfo = tokens[idx];
        if (langInfo.info === 'mermaid') {
            var code = langInfo.content.trim();
            return "<div class=\"embed-mermaid\"><embed-mermaid><pre class=\"zenn-mermaid\">" + md.utils.escapeHtml(code) + "</pre></embed-mermaid></div>";
        }
        return defaultRender(tokens, idx, options, env, slf);
    };
}
exports.mdMermaid = mdMermaid;
